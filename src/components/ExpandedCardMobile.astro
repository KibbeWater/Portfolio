---
import { Image } from 'astro:assets';

interface Props {
	src: ImageMetadata;
	alt: string;
    href: string;
}

const { src, alt, href } = Astro.props;
---

<a href={href} class='w-32 aspect-square'>
    <Image class='h-full w-full rounded-2xl object-cover' src={src} alt={alt} />
</a>

